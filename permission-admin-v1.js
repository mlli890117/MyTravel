/* My Travel Django-style permission admin v1 */
(()=>{
 const sb=()=>window.myTravelAuth,admin=()=>window.myTravelIsSuperAdmin?.()===true,esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let perms=[],groups=[],users=[],selectedUser=null;
 const catalog=[['trip','旅程'],['itinerary','行程'],['flight','航班'],['hotel','住宿'],['expense','記帳'],['check','待辦'],['shop','購物'],['admin','管理']];
 async function load(){if(!admin()||!sb())return;try{const [p,g,u]=await Promise.all([sb().from('permissions').select('*').order('permission_key'),sb().from('permission_groups').select('*,permission_group_permissions(permission_key)').order('name'),sb().functions.invoke('admin-users',{body:{action:'list'}})]);if(p.error)throw p.error;if(g.error)throw g.error;if(u.error)throw u.error;perms=p.data||[];groups=g.data||[];users=u.data?.users||[];render()}catch(e){console.warn('permission admin',e)}}
 function render(){const host=document.getElementById('adminUsersCard');if(!host||!admin())return;let c=document.getElementById('permissionAdminCard');if(!c){c=document.createElement('div');c.id='permissionAdminCard';c.className='card';c.style.marginTop='16px';host.after(c)}c.innerHTML=`<div class="row between"><div><h2>🔐 群組與細項權限</h2><div class="muted">權限可透過群組授予，也可以針對單一使用者額外設定。</div></div><button class="btn" id="paNewGroup">＋新增群組</button></div><div id="paGroups" style="margin-top:14px"></div><hr style="margin:22px 0;border:0;border-top:1px solid #e5e7eb"><div class="row between"><div><h3>使用者個別權限</h3><div class="muted">選擇使用者後，可設定群組與額外權限。</div></div><select id="paUser" style="max-width:280px"><option value="">請選擇使用者</option>${users.filter(x=>x.system_role!=='super_admin').map(x=>`<option value="${x.user_id}">${esc(x.display_name)}</option>`).join('')}</select></div><div id="paUserEditor"></div>`;document.getElementById('paNewGroup').onclick=()=>editGroup();document.getElementById('paUser').onchange=e=>{selectedUser=e.target.value;renderUser()};renderGroups();if(selectedUser){document.getElementById('paUser').value=selectedUser;renderUser()}}
 function renderGroups(){const h=document.getElementById('paGroups');h.innerHTML=groups.length?groups.map(g=>`<div class="setting"><div class="row between"><div><b>${esc(g.name)}</b><div class="muted">${(g.permission_group_permissions||[]).length} 個權限</div></div><div class="row"><button class="btn sm alt" data-ge="${g.id}">編輯</button><button class="btn sm red" data-gd="${g.id}">刪除</button></div></div></div>`).join(''):'<div class="muted">尚未建立群組</div>';h.querySelectorAll('[data-ge]').forEach(b=>b.onclick=()=>editGroup(groups.find(g=>String(g.id)===b.dataset.ge)));h.querySelectorAll('[data-gd]').forEach(b=>b.onclick=()=>deleteGroup(groups.find(g=>String(g.id)===b.dataset.gd)))}
 function modal(title,body,save){let m=document.getElementById('paModal');if(!m){m=document.createElement('div');m.id='paModal';m.className='modal';document.body.appendChild(m)}m.innerHTML=`<div class="modalbox" style="width:min(920px,100%)"><div class="row between"><h2>${title}</h2><button class="btn alt" id="paX">✕</button></div>${body}<div class="row" style="justify-content:flex-end;margin-top:18px"><button class="btn alt" id="paCancel">取消</button><button class="btn" id="paSave">儲存</button></div></div>`;m.classList.add('show');m.querySelector('#paX').onclick=m.querySelector('#paCancel').onclick=()=>m.classList.remove('show');m.querySelector('#paSave').onclick=save;return m}
 function permOptions(selected=[]){const set=new Set(selected);return `<div class="perm-grid" style="display:grid;grid-template-columns:1fr 1fr;gap:14px"><div><b>可用權限</b><div class="muted">勾選要授予的功能。</div></div><div><b>權限被選</b><div class="muted">目前已選權限會保持勾選。</div></div></div><div style="max-height:390px;overflow:auto;margin-top:10px">${catalog.map(([prefix,label])=>{const list=perms.filter(p=>p.permission_key.startsWith(prefix+'.'));return list.length?`<div class="setting"><b>${label}</b>${list.map(p=>`<label style="display:flex;gap:8px;align-items:center;margin-top:8px"><input type="checkbox" class="paPerm" value="${esc(p.permission_key)}" ${set.has(p.permission_key)?'checked':''}> <span>${esc(p.name||p.permission_key)}</span><span class="muted">${esc(p.permission_key)}</span></label>`).join('')}</div>`:''}).join('')}</div>`}
 function editGroup(g=null){const selected=(g?.permission_group_permissions||[]).map(x=>x.permission_key),m=modal(g?'編輯群組':'新增群組',`<div><label>群組名稱</label><input id="paGroupName" value="${esc(g?.name||'')}"></div>${permOptions(selected)}`,async()=>{try{const name=m.querySelector('#paGroupName').value.trim();if(!name)return alert('請輸入群組名稱');let id=g?.id;if(g){const{error}=await sb().from('permission_groups').update({name}).eq('id',id);if(error)throw error}else{const{data,error}=await sb().from('permission_groups').insert({name}).select('id').single();if(error)throw error;id=data.id}const keys=[...m.querySelectorAll('.paPerm:checked')].map(x=>x.value);let r=await sb().from('permission_group_permissions').delete().eq('group_id',id);if(r.error)throw r.error;if(keys.length){r=await sb().from('permission_group_permissions').insert(keys.map(permission_key=>({group_id:id,permission_key})));if(r.error)throw r.error}m.classList.remove('show');await load()}catch(e){alert('儲存失敗：'+e.message)}})}
 async function deleteGroup(g){if(!confirm(`刪除群組「${g.name}」？`))return;const{error}=await sb().from('permission_groups').delete().eq('id',g.id);if(error)return alert(error.message);await load()}
 async function renderUser(){const h=document.getElementById('paUserEditor');if(!h)return;if(!selectedUser){h.innerHTML='';return}h.innerHTML='<div class="muted" style="margin-top:12px">讀取中…</div>';try{const [ug,up]=await Promise.all([sb().from('user_permission_groups').select('group_id').eq('user_id',selectedUser),sb().from('user_permissions').select('permission_key').eq('user_id',selectedUser)]);if(ug.error)throw ug.error;if(up.error)throw up.error;const gs=new Set((ug.data||[]).map(x=>String(x.group_id))),ps=new Set((up.data||[]).map(x=>x.permission_key));h.innerHTML=`<div class="setting" style="margin-top:14px"><b>群組</b>${groups.map(g=>`<label style="display:block;margin-top:8px"><input type="checkbox" class="paUG" value="${g.id}" ${gs.has(String(g.id))?'checked':''}> ${esc(g.name)}</label>`).join('')||'<div class="muted">尚無群組</div>'}</div>${permOptions([...ps])}<div class="row" style="justify-content:flex-end;margin-top:12px"><button class="btn" id="paSaveUser">儲存使用者權限</button></div>`;h.querySelector('#paSaveUser').onclick=async()=>{try{const gids=[...h.querySelectorAll('.paUG:checked')].map(x=>x.value),keys=[...h.querySelectorAll('.paPerm:checked')].map(x=>x.value);let r=await sb().from('user_permission_groups').delete().eq('user_id',selectedUser);if(r.error)throw r.error;r=await sb().from('user_permissions').delete().eq('user_id',selectedUser);if(r.error)throw r.error;if(gids.length){r=await sb().from('user_permission_groups').insert(gids.map(group_id=>({user_id:selectedUser,group_id})));if(r.error)throw r.error}if(keys.length){r=await sb().from('user_permissions').insert(keys.map(permission_key=>({user_id:selectedUser,permission_key})));if(r.error)throw r.error}alert('使用者權限已更新');window.dispatchEvent(new Event('mytravel-permissions-updated'))}catch(e){alert('儲存失敗：'+e.message)}}}catch(e){h.innerHTML='<div class="muted">讀取失敗：'+esc(e.message)+'</div>'}}
 window.addEventListener('mytravel-auth-changed',()=>setTimeout(load,900));window.addEventListener('mytravel-users-updated',load);setTimeout(load,2200);
})();